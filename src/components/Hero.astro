---
import type { Lang } from '../i18n/utils';
import { useTranslations } from '../i18n/utils';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden pt-20">
  <!-- Dynamic Background -->
  <div class="absolute inset-0 bg-white dark:bg-dark-900 transition-colors duration-300">
    <div class="absolute inset-0 bg-grid-pattern opacity-[0.03] dark:opacity-20"></div>
    <!-- Animated Orbs -->
    <div class="glow-orb w-[500px] h-[500px] bg-accent/10 dark:bg-accent/20 top-[-100px] left-[-100px] animate-pulse-slow"></div>
    <div class="glow-orb w-[400px] h-[400px] bg-blue-500/5 dark:bg-blue-500/10 bottom-[0px] right-[-100px] animate-pulse-slow delay-200"></div>
    <div class="glow-orb w-[300px] h-[300px] bg-purple-500/5 dark:bg-purple-500/10 top-[20%] right-[20%] animate-pulse-slow delay-300"></div>
  </div>

  <div class="container-narrow mx-auto px-6 text-center relative z-10">
    <!-- Status Badge -->
    <div class="inline-flex items-center gap-2 px-4 py-2 bg-neutral-100 dark:bg-white/5 border border-neutral-200 dark:border-white/10 backdrop-blur-md rounded-full mb-8 animate-fade-in hover:bg-neutral-200 dark:hover:bg-white/10 transition-colors cursor-default">
      <span class="relative flex h-3 w-3">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent opacity-75"></span>
        <span class="relative inline-flex rounded-full h-3 w-3 bg-accent"></span>
      </span>
      <span class="text-sm font-medium text-neutral-600 dark:text-neutral-300 tracking-wide uppercase">EST. 2010 • Zadar, Croatia</span>
    </div>

    <!-- Main Title -->
    <h1 class="text-5xl md:text-7xl lg:text-8xl font-black text-neutral-900 dark:text-white mb-8 text-balance leading-[0.9] tracking-tight animate-fade-in delay-100">
      <span class="block text-transparent bg-clip-text bg-gradient-to-r from-neutral-900 via-neutral-800 to-neutral-500 dark:from-white dark:via-white dark:to-neutral-400 transition-all duration-300">{t.hero.title}</span>
    </h1>

    <!-- Subtitle -->
    <p class="text-lg md:text-xl text-neutral-600 dark:text-neutral-400 max-w-3xl mx-auto mb-12 text-balance leading-relaxed animate-fade-in delay-200 font-light">
      {t.hero.subtitle}
    </p>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-6 animate-fade-in delay-300">
      <a href="#contact" class="btn-primary group text-white dark:text-dark-900">
        <span>{t.hero.cta}</span>
        <svg class="w-5 h-5 ml-2 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
        </svg>
      </a>
      <a href="#services" class="btn-secondary text-neutral-900 dark:text-white border-neutral-200 dark:border-white/10 hover:bg-neutral-100 dark:hover:bg-white/10">
        {t.nav.services}
      </a>
    </div>

    <!-- Trust Indicators -->
    <div class="mt-24 pt-8 border-t border-neutral-200 dark:border-white/5 grid grid-cols-2 md:grid-cols-4 gap-8 max-w-4xl mx-auto animate-fade-in delay-300">
      <div class="text-center group">
        <div class="text-3xl md:text-4xl font-bold text-neutral-900 dark:text-white mb-1 group-hover:text-accent transition-colors">15+</div>
        <div class="text-xs uppercase tracking-widest text-neutral-500">{t.about.years}</div>
      </div>
      <div class="text-center group">
        <div class="text-3xl md:text-4xl font-bold text-neutral-900 dark:text-white mb-1 group-hover:text-accent transition-colors">24/7</div>
        <div class="text-xs uppercase tracking-widest text-neutral-500">{t.about.support}</div>
      </div>
      <div class="text-center group">
        <div class="text-3xl md:text-4xl font-bold text-neutral-900 dark:text-white mb-1 group-hover:text-accent transition-colors">EU</div>
        <div class="text-xs uppercase tracking-widest text-neutral-500">{t.about.reach}</div>
      </div>
      <div class="text-center group">
        <div class="text-3xl md:text-4xl font-bold text-neutral-900 dark:text-white mb-1 group-hover:text-accent transition-colors">100%</div>
        <div class="text-xs uppercase tracking-widest text-neutral-500">Uptime</div>
      </div>
    </div>
  </div>

  <!-- Scroll Scroll -->
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 animate-bounce hidden md:block opacity-50">
    <div class="w-6 h-10 border-2 border-neutral-500 rounded-full flex justify-center p-1">
      <div class="w-1 h-3 bg-neutral-500 rounded-full animate-scroll"></div>
    </div>
  </div>
</section>
